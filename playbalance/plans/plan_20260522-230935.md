# bal-converge plan — 2026-05-22 23:09

## 1. Context

### 이전 리포트 이력 (최근 4개, 모두 pass 0회)

| 시각 | REASON | 비고 |
|---|---|---|
| 2026-05-22 16:19 | max_iter | KPI=config default. 파이프라인 검증 1차. baseline ~62s |
| 2026-05-22 18:59 | apply_abort_3 | Ghost derived target 자동 발견했으나 dirty asset 충돌 |
| 2026-05-22 19:38 | apply_abort_3 | baseline `20260522_005` (Dur 61.98±0.14, σ/μ=0.2%) — 사실상 structural cutoff. spawn_rate 22회 시도 |
| 2026-05-22 20:10 | user_abort_pre_cycle | KPI=`Dur>=90` 합의됐으나 사이클 진입 전 abort (#3 세션 fix 작업 진입) |

### 관찰 패턴

- **3회 abort 의 진짜 원인 = ML-Agents 240Hz × MaxStep=15000 timer**. #3 세션 fix commit (`5838dd5`) 으로 해결. MaxStep=75000 + cause="death"/"timeout" 분기.
- 이전 baseline (`_005~_022`) 의 ~62s 는 모두 timer 데이터 → 재사용 불가. cause="death" 가 거짓말이었음.
- spawn_rate 가 가장 많이 시도됐으나 Ghost burst (Dense 패턴) 에는 무효 (memory `project_spawnset_pattern_lever`). 이번 사이클은 다른 lever 로 전환.

### Baseline 후보 (PlayTrace 최근)

| Session | N | Dur(s) | Lvl | cause | 판정 |
|---|---|---|---|---|---|
| `_023` | **1** | 64.4 | 4.0 | death(1) | fix 후 첫 play, **N=1 부족** |
| `_022` | 2 | 63.1±1.2 | 1.5 | death(2) | fix 전 timer 오염 |
| `_018~_021` | 2~3 | 62.4±0.7 | 3.0~3.3 | death(전부) | fix 전 timer 오염 |

**→ baseline 재사용 불가. 새 측정 필수.** 새 baseline 예상: Dur ~64s+ (contact death), Lvl 3~4.

## 2. Direction

### KPI
```
Dur>=90
```

이유: baseline ~64s 대비 90s 도달이 1차 검증 목표. 20:10 user_abort 직전 plan 의 KPI 그대로. fix 후 첫 본격 사이클이므로 도달 가능성이 가장 높은 단일 metric 선택. Lvl 조건은 이번 라운드 생략 (Dur 가 풀리면 자연스럽게 따라옴).

### Baseline
**새 측정** (`/bal-converge` 진입 시 자체 baseline 단계에서 N=5 실측).

### Knob whitelist (의도)

허용 (3개):
- **`spawn_burst_count`** — Ghost Dense Count=30 직접 축소. Ghost 가 60s burst structural cutoff 의 핵심이라면 이 lever 가 가장 직접적 (memory `project_ghost_burst_structural_cutoff`).
- **`enemy_damage`** — 적 한 방 데미지. baseline 의 contact death 임팩트 약화. **주의: 정수 round-trip 흡수** (memory `project_ghost_burst_structural_cutoff` 의 trap 항목, 작은 정수에 ±15% 가 반올림으로 흡수). 첫 진단 결과 변경 없으면 max_iter 1회 더 소비 예상.
- **`enemy_hp`** — 적 체력. 모델 살상 속도 향상으로 burst 사이 정리 시간 확보 (간접 lever).

이번 라운드 제외 (이유):
- `spawn_rate` — Dense/Circle 패턴 (Ghost) 에 effect 없음 (memory `project_spawnset_pattern_lever`). Random 패턴 한정.
- `spawn_intensity_curve` — AnimationCurve manual 편집 대상, auto apply 불가.
- `level_up_xp_increment` / `player_max_hp` — 이번 라운드 가설 (spawn 구조) 과 직접 관계 약함. 다음 사이클로 보류.

⚠️ **whitelist 강제 메커니즘 없음**: bal-apply 는 진단 결과로 knob 을 자동 선택한다. 제외 항목이 선택되는 것을 막으려면 `/bal-converge` 실행 전 `.claude/bal-apply.json` 의 해당 knob 항목을 일시 제거하거나, 사이클 후 회고에서 swap 여부 확인. **권장: 그대로 진행하고 결과 회고에서 trap 인지** (수정 부담 < 회고 부담).

### 예산
- `N=5`, `max_iter=3`, `per-play timeout=10m`, `max_wall=90m`
- 실제 한 판 ~5분 (MaxStep=75000 × 240Hz × timeScale=1, timer cap)
- 예상 wall-time ≈ 3 × (5 × 5분 + apply ~30s) ≈ **80~90분**

**Timeout 근거**: baseline=new 이라 데이터 없음 → config default 10m 유지. timer cap (~5.2분) 보다 충분히 길어서 정상 케이스 영향 0, hang 감지용 안전망.

**max_wall 근거**: config default 30분이 예상 80~90분 사이클과 충돌. `max_wall=90m` override 로 자연 종료 허용.

## 3. Hypothesis priority

1. **Ghost Dense burst 가 진짜 60s structural cutoff 다.** spawn_burst_count 축소만으로 Dur 90s 돌파 가능. 검증 시 다른 knob 불필요.
2. **모델의 contact death 가 burst 와 무관한 일반 사망이다.** 이 경우 enemy_damage / enemy_hp 가 효과 가져옴. spawn_burst_count 변경 후에도 Dur 정체면 가설 1 기각.
3. (잔존) timer fix 후 모델이 학습 모드와 다른 Hz 환경에서 동작 → OOD 행동 가능. baseline Dur 분산이 크면 (σ/μ > 10%) 가설 점검 필요.

## 4. Traps (주의)

- **enemy_damage 정수 round-trip 흡수**: baseline Enemy SO 의 Attack 값이 작은 정수 (예: 3) 이면 ±15% 가 반올림 흡수. iter 1 에서 진단됐으나 변경 0건이면 무효 한 사이클 소비. adjust_default 를 -0.30 이상으로 키우는 옵션 고려 (.claude/bal-apply.json 수정).
- **spawn_burst_count 가 신설된 knob**: 지난 세션에 추가됨 (commit `0b1de34`). 첫 본격 사용. asset 경로 매칭 / field_path 가 의도대로 동작하는지 iter 1 의 git diff 에서 확인 필수.
- **baseline σ/μ precondition** (memory `feedback_baseline_stability_precondition`): 새 baseline N=5 측정 결과 σ/μ > 10% 면 cycle 진입 직전 abort 가능. 첫 일관성 점검.
- **dashboard pane 반사적 점검** (memory `feedback_bal_dashboard_panes_reflexive`): CONVERGE + GAME pane (pane:50, surface:52, surface:53) 살아있는지 사이클 시작 직전 확인.

## 5. Recommended command

```
/bal-converge kpi="Dur>=90" N=5 max_iter=3 timeout=10m max_wall=90m
```

## 6. Post-run checklist

- [ ] HTML 리포트 `playbalance/reports/converge_<ts>_<reason>.html` 생성됐는지
- [ ] REASON 이 `pass` 면 git diff 로 적용된 자산 확인 후 commit
- [ ] REASON 이 `max_iter` / `diverged` / `apply_abort_3` 면 어떤 knob 이 선택됐는지 + trap (enemy_damage round-trip, spawn_burst_count field_path) 점검
- [ ] 새 baseline Dur 가 ~64s 부근 (fix 검증값) 인지 확인. 만약 ~62s 면 fix 가 inference 모델에 안 들어간 것
- [ ] HTML 의 `episode.cause` 분포에서 "timeout" 항목이 0건인지 (fix 검증)
- [ ] memory 갱신: spawn_burst_count 가 효과 있었으면 `project_ghost_burst_structural_cutoff` 의 fix 결과에 lever 효과 추가
