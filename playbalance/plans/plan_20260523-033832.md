# bal-converge plan — 2026-05-23 03:38 (역방향: XP 가속 검증)

## 1. Context

### 이전 converge 이력

| Time | Reason | 요지 |
|---|---|---|
| 2026-05-23 03:30 | **diverged** | KPI Dur 195~225. Gear -50% 가 XP 끊어 Lvl 12.6→4.8, Dur 144→86s. 양방향 가설 무효 입증. |
| 2026-05-23 01:02 | user_abort_after_iter1 | iter 1 fast-death 패턴 감지 후 사용자 중단 |
| 2026-05-22 23:59 | **PASS** | KPI Dur 180~240 AND Lvl>=5. Ghost Count 30→15 단독, iter 1, Dur 79→164s |
| 2026-05-22 19:38 | apply_abort_3 | enemy_damage 정수 round-trip |

### 직전 cycle 의 결정적 발견

- **memory `bimodal_kill_threshold`** 의 "90s wall = snowball 임계" 정량 검증: Lvl 4.8 인 모델은 80s 안에 무조건 사망. Lvl 12.6 였던 baseline 의 절반 시간에 즉사.
- **인과 체인 확정**: early spawn ↓ → 잡을 적 ↓ → XP ↓ → Lvl ↓ → 무기 약함 → mid-game 적 못 잡음 → 사망.
- **방향 전환**: spawn 자체 조절이 아니라 **레벨업 가속** 으로 snowball 임계 통과 보장이 우선.

### Baseline 후보 (PlayTrace 최근 7일, 현재 자산 상태와 일치하는 것)

| Session | N | Dur (mean ± σ) | max | Lvl | σ/μ | 비고 |
|---|---|---|---|---|---|---|
| 20260523_003 | 5 | 83.9 ± 7.6s | 92 | ~3 | 9% | 현재 자산 상태 stable baseline |
| 20260522_024 | 5 | 79.5 ± 7.3s | 89 | ~3 | 9% | pre-fix (Count=30) |
| 20260523_009 | 5 | 85.9 ± 8.5s | 95 | **4.8** | 10% | diverged 결과 (참고) |
| 20260523_005 | 5 | 143.9 ± 123s | 364 | 12.6 | 86% | bimodal (제외 — level_cap_low 발화 안 함) |

→ 새 측정으로 갱신 (현재 자산 = 20260523_003 와 동일 환경, 7분 소요)

## 2. Direction

### KPI

`Dur 180~240 AND Lvl>=5`

이유: 2026-05-22 23:59 PASS 와 동일 KPI. **Lvl>=5 게이트**가 핵심 — XP 가설을 직접 검증한다 (현재 Lvl ~3, 임계 통과 시 Lvl 7+ 으로 점프). 범위 180~240 은 직전 PASS 의 mean 164s 기준 살짝 위 — 이번엔 더 안정적 도달 목표.

### Baseline

새 측정 (사이클 시작 시 N=5).

### Knob whitelist

**단일 knob: `level_up_xp_increment`** (adjust_default=-0.2, range [-0.5, +0.5])

자산: `Assets/RGame/RoguelikeKit/ScriptableObjects/DataSO/Exp/ExpConfig.asset` field `experienceIncrement`.

효과: 레벨업에 필요한 추가 XP 가 -20% (예: lvl1→2 가 50 EXP 필요했다면 40 EXP 로). 같은 적 잡고도 레벨 더 빠르게.

**다른 knob 제외 필요** — bal-converge 가 진단으로 자동 매칭하므로 다음 finding 들이 의도와 다른 knob 을 발화시킬 위험:

| Finding | 매칭 knob | 위험 |
|---|---|---|
| killer_concentration | enemy_damage | 정수 round-trip no-op 또는 의도 분산 |
| structural_duration_cutoff | spawn_burst_count | Ghost Count 추가 down (memory: 15<OOD) |
| low_kill_rate | enemy_hp | XP gain 가속 보조이지만 가설 분리 망침 |
| high_damage_taken_variance | player_max_hp | 디버그 흐림 |

### Knob 격리 방법 (사이클 시작 전 1회)

bal-converge 는 `whitelist=` 인자 없음 — `.claude/bal-apply.json` 의 knob 들 `triggers` 를 수동 수정.

**옵션 A (권장): 영구 수정 안 함, temporary patch**
```bash
# 1. 임시 백업
cp .claude/bal-apply.json .claude/bal-apply.json.bak
# 2. level_up_xp_increment 외 knob 의 triggers 를 [] 로
jq '.knobs |= map(if .id == "level_up_xp_increment" then . else .triggers = [] end)' \
   .claude/bal-apply.json.bak > .claude/bal-apply.json
# 3. /bal-converge 실행
# 4. 사이클 종료 후 복원
mv .claude/bal-apply.json.bak .claude/bal-apply.json
```

**옵션 B: 수동 편집** — 편집기로 다른 9개 knob 의 `"triggers": [...]` 를 `"triggers": []` 로. 사이클 끝나면 git checkout 으로 복원.

### 예산

N=5, max_iter=3, per-play timeout=10m. 예상 wall-time ≈ 3 × (5 × ~2~3min + apply 30s) ≈ **30~45분**.

**Timeout 근거**: 현재 환경 baseline 후보 max Dur 91.5s. 가설 검증되면 mean ~200s 까지 상승, max 가 250~280s 가능. 10m=600s 는 hang 감지용 안전망 (정상 케이스에선 hit 안 됨).

## 3. Hypothesis priority

1. **iter 1**: ExpConfig.experienceIncrement -20% 단독 적용 → Lvl 4 → ~6+ 으로 상승 + Dur 84s → ~130s+ 도달 예상. KPI 부분 통과 (Lvl>=5) 또는 distance 감소.
2. **iter 2~3**: Lvl>=5 통과했으나 Dur<180 이면 iter 2 에서 추가 -20% (누적 -36%) 시도. 또는 Dur 충분히 올라갔는데 여전히 Lvl<5 이면 그건 모델의 XP-action 정책 문제 (knob 으로 못 풀음).
3. **반증 케이스**: -40% 적용해도 Lvl<5 잔존 → XP 가속이 충분치 않음 → enemy_hp 도 묶어 처치 속도 가속 시도 (별 plan).

## 4. Traps

- **`bimodal_kill_threshold`**: 90s wall 의 snowball 임계. 이번 가설이 정확히 이걸 깨는 게 목표. iter 1 mean Dur 가 90s 초과하면 가설 부분 성공.
- **`project_ghost_burst_structural_cutoff`**: Ghost Count=15 현 환경에서 spawn_burst_count knob 가 추가 down 시도하면 OOD 위험. → triggers=[] 처리로 격리.
- **`project_spawnset_asset_window_verification`**: 자산 stem 만으로 시간대 분류 금지. early_spawn_rate / late_spawn_* 도 격리해 재발 방지.
- **ExpConfig 정수 함정**: `experienceIncrement` 가 정수인지 float 인지 확인 필요. -20% 가 round-trip 흡수되면 no-op. 사이클 시작 직후 `git diff` 로 실 변경 확인.
- **session 20260523_009 (망친 환경) 가 PlayTrace 최신 session 으로 남음**: bal-converge 가 자동 baseline 선택 시 이걸 집을 위험. 새 측정으로 명시.

## 5. Recommended commands

### Step 1: knob 격리 (사이클 시작 전, 1회)

```bash
cp .claude/bal-apply.json .claude/bal-apply.json.bak
jq '.knobs |= map(if .id == "level_up_xp_increment" then . else .triggers = [] end)' \
   .claude/bal-apply.json.bak > .claude/bal-apply.json
```

### Step 2: 사이클 실행

```
/bal-converge kpi="Dur 180~240 AND Lvl>=5" N=5 max_iter=3 timeout=10m
```

### Step 3: 사이클 종료 후 복원

```bash
mv .claude/bal-apply.json.bak .claude/bal-apply.json
```

## 6. Post-run checklist

- [ ] HTML 리포트가 `playbalance/reports/converge_<ts>_<reason>.html` 에 생성됐는지
- [ ] REASON pass / max_iter / no_op / apply_abort_2 중 어떤 것인지
- [ ] git diff — ExpConfig.asset 만 변경됐는지 (≤1 자산, 다른 자산 변경 시 격리 실패)
- [ ] mean Dur 변화 (84s → ?) 및 mean Lvl 변화 (3 → ?)
- [ ] σ/μ 가 안정적 stable 유지 (≤15%) 또는 bimodal 로 분기됐는지
- [ ] knob 격리 복원 (`mv .bak`) 잊지 않았는지 — `git status .claude/bal-apply.json` 으로 확인
